{% extends "base.twig" %}

{% block content %}
    {% if posts %}
        <div class="flex flex-wrap -mx-2">
            {% for post in posts %}
                {% include ['partials/tease-'~post.format~'.twig', 'partials/tease-standard.twig'] %}
            {% endfor %}
        </div>
        {% include 'partials/pagination.twig' with { pagination: posts.pagination({show_all: false, mid_size: 3, end_size: 2}) } %}
    {% endif %}
{% endblock %}