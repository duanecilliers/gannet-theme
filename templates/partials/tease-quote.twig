<div class='px-2 w-full sm:w-1/2 lg:w-1/3 xl:w-1/4 mb-5'>
    <article id="post-{{ post.id }}" class="{{ post.class }} h-full card--quote">
        {% block content %}
            <div class="card-content">
                {{post.content}}
            </div>
        {% endblock %}
    </article>
</div>